datasource db {
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}


model Todo {
  id          Int      @id @default(autoincrement())
  text        String
  description String
  status      String
  created_at  DateTime @default(now())
}
